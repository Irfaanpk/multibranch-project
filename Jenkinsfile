pipeline {
    agent any

    stages {
        stage('Dev Branch Stage') {
            steps {
                bat 'echo This is the DEV branch build.'
                bat 'echo Development environment pipeline executing...'
            }
        }
    }
}
